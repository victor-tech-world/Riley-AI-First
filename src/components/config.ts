export const businessFirst:{title:string, content:string} =
 {title:'From fragmented data to a single operating view', content:'Customer, product, and revenue signals are continuously reconciled into one trusted business model, so every team works from the same reality'};
export const businessSecond:string[] = ['Revenue risk identified early', 'Churn, renewal, and expansion signals surface months in advance, so teams know which customers to save, grow, or ignore before revenue decisions are forced'];
export const businessThird: string[] = ['Decisions tested before resources are committed', 'Pricing, packaging, GTM, and product bets are pressure-tested against retention and expansion impact, so teams stop guessing and commit capital where growth is most likely'];
export const businessForth: string[] = ['Insights that drive execution', 'The right teams receive prioritized, decision-ready insights at the right moment, so analysis turns into action, not dashboards'];
